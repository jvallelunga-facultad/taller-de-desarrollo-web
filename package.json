{
  "name": "root",
  "private": true,
  "scripts": {
    "start:api": "docker-compose up",
    "start:webapp": "npx lerna run --scope webapp development",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^5.0.0"
  }
}
